## 1. OA 多租户收口

- [x] 1.1 为 OA repository 和 service 补齐 `tenantId` 入参与写入逻辑，确保申请创建、审批、检索、公告和通讯录全部按当前租户过滤。
- [x] 1.2 修正 OA 默认审批人解析逻辑，确保按当前租户与权限联合筛选审批候选人。
- [x] 1.3 补充 OA 相关测试，覆盖跨租户审批、跨租户公告/通讯录读取被拒绝的场景。

## 2. Workflow 与开放平台仓储契约收紧

- [x] 2.1 为 workflow repository 和 service 补齐租户约束，确保模板、实例、任务、抄送和审批人解析都按当前租户收口。
- [x] 2.2 为开放平台更新链路补齐 repository 层的对象标识与租户标识联合约束，避免仅按主键更新租户级对象。
- [x] 2.3 补充 workflow 与开放平台相关测试，覆盖跨租户模板读取、流程推进和租户级集成对象更新。

## 3. 宿主与子应用契约对齐

- [x] 3.1 对齐主应用导航项与 OA/SCRM 子应用路由的页面权限键，消除同一路径的权限漂移。
- [x] 3.2 统一 OA 与 SCRM 在宿主模式下的登录失效跳转逻辑，确保重新登录始终回到主应用 `/login`。
- [x] 3.3 调整租户级系统管理页面与平台级治理接口边界，隐藏或移除不应暴露给租户管理员的治理入口。

## 4. 验证与文档

- [x] 4.1 运行后端与前端相关测试，验证多租户、权限契约和微前端登录跳转修复。
- [x] 4.2 更新 README 或运维说明，记录这次边界收紧后的宿主登录和平台级治理入口约束。
