## 1. 主应用工作区与依赖

- [x] 1.1 新建 `apps/main-web` 工作区，补齐 `package.json`、`tsconfig`、`vite` 入口、根脚本与环境接入。
- [x] 1.2 安装并配置 `qiankun` 及子应用接入所需依赖，建立主应用的子应用注册与运行时模块。

## 2. 主应用壳层与视觉系统

- [x] 2.1 基于 `ui-ux-pro-max` 设计系统实现主应用的全局样式令牌、登录页、统一导航壳层、页面标题区和子应用容器态。
- [x] 2.2 实现主应用菜单注册表、统一权限过滤、首个可访问页面兜底和 OA/SCRM 域分组导航。

## 3. OA 子应用接入

- [x] 3.1 改造 `apps/oa-web` 入口、router 与 runtime，使其同时支持独立运行和 `qiankun` 子应用模式。
- [x] 3.2 改造 `apps/oa-web` 布局，在宿主模式下隐藏重复导航壳层，仅保留内容区并确保页面可在主应用中正常展示。

## 4. SCRM 子应用接入

- [x] 4.1 改造 `apps/scrm-web` 入口、router 与 runtime，使其同时支持独立运行和 `qiankun` 子应用模式。
- [x] 4.2 改造 `apps/scrm-web` 布局，在宿主模式下隐藏重复导航壳层，仅保留内容区并确保页面可在主应用中正常展示。

## 5. 文档与验证

- [x] 5.1 更新根级 README、开发脚本与必要环境说明，明确主应用联调与子应用独立调试方式。
- [x] 5.2 运行并通过与本次改动相关的安装、类型检查、测试、构建或联调验证，并在 OpenSpec 任务中同步勾选完成项。
