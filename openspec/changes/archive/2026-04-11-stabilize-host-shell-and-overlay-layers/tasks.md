## 1. 主应用壳层规范整理

- [x] 1.1 归纳 `main-web` 壳层改动涉及的当前业务域识别、导航结构与移动端菜单语义。
- [x] 1.2 为 `backoffice-super-app-shell` 与 `backoffice-visual-language` 编写并同步对应 delta specs。

## 2. 宿主模式弹层规范整理

- [x] 2.1 归纳 `scrm-web` 中因宿主模式补充 `append-to-body` 的对话框与抽屉范围。
- [x] 2.2 为 `access-control`、`system-administration`、`customer-management`、`lead-followup` 与 `sales-opportunity-management` 编写对应 delta specs。

## 3. 归档与提交

- [x] 3.1 运行 OpenSpec 校验并将本次 change 归档回主 specs。
- [x] 3.2 将代码改动与 OpenSpec 产物整理为一次 git 提交。
