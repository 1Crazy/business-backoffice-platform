## 1. 导航、权限与壳层契约

- [x] 1.1 梳理主应用导航项与 OA/SCRM 子应用路由，生成页面契约对照表，覆盖 path、title、permission、hidden、microAppName。
- [x] 1.2 增加契约测试，确保宿主菜单权限与子应用页面权限一致，缺失或漂移时直接失败。
- [x] 1.3 评估是否抽出共享页面契约模块；若暂不抽模块，则在测试中锁定重复配置的一致性。
- [x] 1.4 补充登录失效和无权限跳转测试，确保 host、OA、SCRM 在嵌入态与独立态行为一致。

## 2. 展示体验与微前端稳定性

- [x] 2.1 调整主应用、OA、SCRM 内容区滚动策略，保留可发现滚动反馈并验证长表格、长表单、抽屉场景。
- [x] 2.2 收敛嵌入态布局 CSS，确保子应用在 host 容器内不会出现高度塌陷、横向溢出或顶栏遮挡。
- [x] 2.3 为 qiankun 加载失败态增加更可诊断的信息，包括应用名、entry、错误摘要和重试入口。
- [x] 2.4 建立最小视觉验证流程，覆盖桌面、移动端、host 嵌入 OA、host 嵌入 SCRM、standalone OA/SCRM。

## 3. Webhook 测试语义修复

- [x] 3.1 将当前 Webhook 字符串模拟结果标记为模拟，避免在 API 和 UI 上误导为真实投递。
- [x] 3.2 设计并实现真实 Webhook 测试投递路径，包含协议限制、内网地址拦截、超时、重定向限制和响应体裁剪。
- [x] 3.3 在系统管理页面展示真实投递状态、状态码、耗时、错误原因和最近投递时间。
- [x] 3.4 补充 Webhook 测试，覆盖模拟模式、真实成功、真实失败、非法 URL、内网 URL 被拒。

## 4. 工程质量护栏

- [x] 4.1 为后端高风险模块建立最小测试基线：auth、open-integration、uploads、tenant-boundary。
- [x] 4.2 为前端壳层建立最小测试基线：主导航、移动导航、微前端错误态、无权限兜底。
- [x] 4.3 将关键验证命令写入 README 或开发文档，明确每次安全/展示相关变更至少运行哪些命令。
- [x] 4.4 运行 `pnpm -r lint`、`pnpm -r test`，并记录无法自动化覆盖的手动视觉验证结果。
