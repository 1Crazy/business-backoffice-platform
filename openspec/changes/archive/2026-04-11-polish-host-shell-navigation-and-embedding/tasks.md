## 1. 主应用导航语义收敛

- [x] 1.1 移除主应用桌面侧边导航父级菜单上的域前缀徽标，仅保留业务语义标签。
- [x] 1.2 清理主应用导航分组中不再使用的域徽标结构与对应测试约束。

## 2. 壳层滚动行为修复

- [x] 2.1 调整主应用微应用挂载容器高度策略，避免宿主内容区产生空滚动。
- [x] 2.2 调整 OA、SCRM 子应用嵌入态根节点高度策略，保持独立模式与宿主模式行为分离。
- [x] 2.3 统一主应用、OA、SCRM 侧边导航的滚动反馈显隐行为。

## 3. 规范同步与验证

- [x] 3.1 为 `backoffice-super-app-shell` 与 `backoffice-visual-language` 编写并同步 delta specs。
- [x] 3.2 运行相关单测与浏览器验证，确认菜单命名、滚动条反馈与宿主嵌入滚动行为符合预期。
