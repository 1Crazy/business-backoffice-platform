## 1. Schema And Shared Contracts

- [x] 1.1 为角色数据范围、用户会话和附件存储元数据补充 Prisma schema 与数据库迁移。
- [x] 1.2 更新种子数据与共享类型定义，为现有系统角色写入默认数据范围并补充分页响应契约。
- [x] 1.3 在 API 公共层新增统一的分页查询 DTO、排序校验和分页响应组装工具。

## 2. Auth And Data Scope Infrastructure

- [x] 2.1 实现共享的数据范围解析器，支持本人、本部门、子部门和全局范围的部门与归属人解析。
- [x] 2.2 重构鉴权链路，在受保护请求中校验用户状态、会话状态和数据范围上下文。
- [x] 2.3 实现登录、刷新访问令牌和退出当前会话接口，并补充失败登录审计记录。
- [x] 2.4 为数据范围解析、会话刷新、会话撤销和停用账号失效补充后端测试。

## 3. Business Modules

- [x] 3.1 改造客户模块，使客户列表返回分页结果，并在列表、详情、跟进、附件和归属变更中复用统一数据范围过滤。
- [x] 3.2 改造线索与提醒模块，使线索列表和待办提醒返回分页结果，并在详情、转化和提醒查询中复用统一数据范围过滤。
- [x] 3.3 改造看板聚合逻辑，使新增客户、跟进次数、转化数、线索总数和待处理提醒统一遵守新的统计口径与数据范围规则。
- [x] 3.4 同步更新相关 controller DTO 和 Swagger 文档，确保新查询参数与返回结构一致。

## 4. Audit And Attachment Hardening

- [x] 4.1 改造审计日志模块，支持按操作类型、对象类型、操作人和时间范围分页筛选，并记录失败认证事件。
- [x] 4.2 抽象附件存储驱动并保留本地磁盘默认实现，同时增加文件大小和类型约束。
- [x] 4.3 新增受业务权限保护的附件下载接口，并在上传与下载链路中落存储提供方和存储键元数据。
- [x] 4.4 为审计分页、附件约束和附件下载权限补充后端测试。

## 5. Web Console Updates

- [x] 5.1 更新前端认证状态管理，支持访问令牌续期、主动退出和会话失效后的恢复流程。
- [x] 5.2 更新客户、线索、提醒和审计日志页面，使其消费分页 API 并保留筛选、排序和翻页状态。
- [x] 5.3 更新看板和相关页面提示，使数据范围变化后的统计与空态提示保持清晰一致。
- [x] 5.4 为前端分页列表和会话生命周期补充关键交互测试。

## 6. Delivery And Rollout

- [x] 6.1 新增仓库级 CI 工作流，执行依赖安装、类型检查、测试和构建校验。
- [x] 6.2 更新 README、环境变量模板和开发文档，补充会话配置、附件约束和新的验证步骤。
- [x] 6.3 完成一次本地全链路验证，覆盖迁移、种子、前后端启动、lint、test 和关键角色权限场景。
