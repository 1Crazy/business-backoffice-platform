## 1. 平台治理域信息架构

- [x] 1.1 在 `main-web` 中新增 `platform` 业务域及 `/platform/**` 路由空间，补齐域上下文、页面标题和无权限兜底规则。
- [x] 1.2 调整主应用导航分组与菜单文案，把现有组织/员工/角色治理入口从 SCRM 分组迁移到平台治理分组。
- [x] 1.3 为平台治理首批入口补齐页面骨架与路由组织，至少覆盖部门治理、员工治理和角色授权治理。

## 2. 治理页面迁移与前端适配

- [x] 2.1 将现有 `access-control` 页面能力迁移或复用到平台治理域，确保部门、员工和角色治理不再依赖 SCRM 产品语义。
- [x] 2.2 调整治理页面中的标题、说明、面包屑和交互文案，使其统一表达平台治理而不是 SCRM 配置页。
- [x] 2.3 为旧的 `/scrm/departments` 等历史入口提供跳转或兼容别名，避免现有访问路径立即失效。

## 3. 权限归属与后端契约收敛

- [x] 3.1 将组织、员工、角色等治理权限的 `appCode` 从 `scrm` 迁移为 `platform`，并同步更新 seed、权限目录接口与测试数据。
- [x] 3.2 更新角色授权界面的权限分组展示，使治理权限按 `platform`、OA、SCRM 三类应用归属展示。
- [x] 3.3 校正平台治理域的页面权限判断与导航过滤逻辑，确保“只有业务域权限、没有平台治理权限”的账号仍能正常进入 OA 或 SCRM。

## 4. 验证与收尾

- [x] 4.1 为平台治理域导航、旧入口跳转、权限分组迁移和无权限兜底补充关键前端测试。
- [x] 4.2 为治理权限 `appCode` 迁移、角色授权分组和平台治理访问控制补充关键后端测试。
- [x] 4.3 运行并通过与 `main-web`、`scrm-web`、`oa-web` 和 `apps/api` 相关的构建、测试及关键联调校验。
