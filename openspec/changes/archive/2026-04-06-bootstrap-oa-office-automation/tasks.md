## 1. 平台级权限与会话基础

- [x] 1.1 为权限目录补充应用命名空间建模，并将现有 SCRM 权限与新增 OA 权限通过 seed 明确归属到各自应用。
- [x] 1.2 更新角色/权限相关 API、VO 与前端角色管理界面，使权限目录能够按应用维度分组展示和授权。
- [x] 1.3 将前端登录态存储从应用专属命名迁移为平台级命名，并确认 `scrm-web` 与 `oa-web` 共用统一身份与续期链路。

## 2. OA 前端应用骨架

- [x] 2.1 初始化 `apps/oa-web` 工作区，补齐包名、根脚本、开发文档和构建入口。
- [x] 2.2 基于 OA 设计系统实现全局样式 token、应用布局壳层、导航结构、无权限兜底页面和路由守卫。
- [x] 2.3 实现 OA 首批页面骨架，包括工作台、待我审批、我发起的申请、请假申请、公告列表/详情和组织通讯录。

## 3. OA 后端模块与首个业务闭环

- [x] 3.1 设计并实现请假申请、审批动作轨迹及相关状态模型，保持 `apps/api` 模块化单体结构不变。
- [x] 3.2 提供审批中心与请假申请所需的后端接口，至少覆盖发起申请、查看我的申请、查看待审批列表和执行审批动作。
- [x] 3.3 提供公告通知与 OA 工作台摘要所需接口，并复用部门/员工基础数据支撑通讯录展示。

## 4. 质量与就绪度

- [x] 4.1 为多应用权限命名空间、请假审批状态流转和 OA 路由守卫补充关键测试。
- [x] 4.2 运行并通过 `lint`、`test`、`build` 以及与 `oa-web` 接入相关的关键联调校验。
