## 1. Shared Visual System

- [x] 1.1 在 `apps/oa-web` 与 `apps/scrm-web` 中梳理并统一语义化设计令牌，建立共享的颜色、表面、边框、圆角、阴影、间距与状态样式变量
- [x] 1.2 为两个应用增加应用级主题覆盖入口，确保共享壳层规则与 OA/SCRM 的主题差异可以并存
- [x] 1.3 统一升级登录页、应用壳层、侧边导航、顶部栏与内容容器的结构和基础交互样式
- [x] 1.4 为通用卡片、筛选容器、状态标签、空态与基础按钮补齐可复用的视觉基线

## 2. OA Experience Refresh

- [x] 2.1 将 `oa-web` 登录页改造成更符合办公门户定位的双栏首屏，强化系统身份、价值说明与统一身份入口
- [x] 2.2 重构 `oa-web` 的布局壳层，使品牌区、面包屑、标题区、用户上下文区和内容区域层级更清晰
- [x] 2.3 升级 OA 工作台首屏模块，突出今日待办、快捷动作、公告摘要和办公信息优先级
- [x] 2.4 将 OA 的列表页、详情页与状态展示样式收敛到新的门户化视觉基线，避免首页与二级页断层

## 3. SCRM Experience Refresh

- [x] 3.1 将 `scrm-web` 登录页改造成更符合销售运营控制台定位的首屏体验，强化数据与业务闭环语义
- [x] 3.2 重构 `scrm-web` 的布局壳层，使导航、页面标题、用户区和内容容器更符合高信息密度后台节奏
- [x] 3.3 升级 SCRM 运营看板的筛选条、KPI 卡、范围提示和洞察模块，形成更明确的数据控制台结构
- [x] 3.4 将 SCRM 常用业务容器样式扩展到客户、线索和系统管理等页面，保证主题一致性

## 4. Verification

- [x] 4.1 为关键布局与首页页面补充或更新必要的组件测试，覆盖主题类名/结构与核心文案渲染
- [x] 4.2 使用浏览器在桌面与窄屏宽度下检查 OA 与 SCRM 的登录页和首页，确认层级、对比度、焦点态与断点行为符合预期
- [x] 4.3 记录并修正浏览器验收中发现的样式回归问题，确保本次视觉升级可作为后续页面迁移基线
